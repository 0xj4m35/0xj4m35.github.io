# GitHub Pages static export

We will publish the CV as a Next.js App Router site using `output: "export"` and deploy the generated `out` artifact to GitHub Pages from the `main` branch. This favors simple public hosting, low operational overhead, and direct compatibility with a `0xj4m35.github.io` user site over server-rendered features that GitHub Pages cannot run.
