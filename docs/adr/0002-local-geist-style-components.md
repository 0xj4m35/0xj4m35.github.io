# Local Geist-style CV components

We will keep the CV as a document-style page and apply Vercel Geist visual language through local components, CSS tokens, Geist fonts, badges, borders, and restrained buttons instead of adopting the archived `geist-ui` React package. This keeps the public CV lightweight and maintainable while avoiding a stale component dependency.
